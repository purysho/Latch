[package]
name = "latch-fs"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
latch-approvals = { path = "../latch-approvals" }
base64 = "0.22"
hex = "0.4"
latch-audit = { path = "../latch-audit" }
latch-core = { path = "../latch-core" }
serde_json = "1"
sha2 = "0.10"

[dev-dependencies]
tempfile = "3"
